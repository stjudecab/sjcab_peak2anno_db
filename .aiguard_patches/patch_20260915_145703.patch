diff --git a/pyproject.toml b/pyproject.toml
index 5a4b687..61448bc 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -40,6 +40,12 @@ include-package-data = true
 [tool.versioningit]
 default-version = "0.0.0"
 
+[tool.versioningit.format]
+# Keep the latest tag when only uncommitted working-tree changes are present.
+distance = "{base_version}"
+dirty = "{base_version}"
+distance-dirty = "{base_version}"
+
 [tool.versioningit.write]
 file = "src/sjcab_peak2anno_db/_generated_version.py"
 
diff --git a/src/sjcab_peak2anno_db/_version.py b/src/sjcab_peak2anno_db/_version.py
new file mode 100644
index 0000000..8835950
--- /dev/null
+++ b/src/sjcab_peak2anno_db/_version.py
@@ -0,0 +1,12 @@
+"""Load the package version generated by versioningit during builds."""
+
+from importlib.metadata import PackageNotFoundError, version
+
+try:
+    from ._generated_version import __version__
+except ImportError:
+    try:
+        __version__ = version("sjcab_peak2anno_db")
+    except PackageNotFoundError:
+        # Keep imports working directly from a source checkout before installation.
+        __version__ = "0.0.0"
